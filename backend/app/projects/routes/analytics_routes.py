from fastapi import APIRouter

# Placeholder for future project analytics endpoints (e.g. reports/statistics
# across projects). No feature has specified this behavior yet — this router
# is intentionally empty until such a feature is specified and planned.
router = APIRouter(prefix="/projects", tags=["projects", "analytics"])
