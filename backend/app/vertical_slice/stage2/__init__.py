"""Stage 2 (Issue #133): the shared contract every later Stage 2 child implements.

See ``contract.py`` for the types and ``docs/stage2/CONTRACT.md`` for the prose. This package
defines vocabulary only — no realization behaviour, no wiring into the production pipeline.
"""
